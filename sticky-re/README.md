Sticky Random Events
-----

If the page is scrolled past where a random event would be visible, this will show it for a few seconds at the top of the window.

Useful, for example, when using the neoboards.
